# =============================================================================
# CardExchange Frontend – Environment Variables
# Copy this file to .env and fill in the values for your environment
# =============================================================================

# Backend API base URL (used for SignalR hub connections)
# In development this is not needed — Vite proxy handles /api requests
# In production set this to the full backend URL (no trailing slash)
VITE_API_URL=http://localhost:8080
